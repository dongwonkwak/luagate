# Admin API base URL override.
# - Dev (default): omit or leave empty — Vite proxy forwards /api to localhost:9090
# - Prod: set to "" (empty string) when Admin API is served on the same origin,
#   or to a full URL like "https://admin.example.com/api"
# VITE_ADMIN_API_URL=/api
