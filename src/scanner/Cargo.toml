[package]
name = "luagate-scanner"
version = "0.1.0"
edition = "2021"

[lib]
name = "luagate_scanner"
crate-type = ["cdylib", "lib"]

[dependencies]
regex = "1.9"
once_cell = "1.18"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
