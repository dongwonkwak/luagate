[package]
name = "luagate-stream"
version = "0.1.0"
edition = "2021"

[lib]
name = "luagate_stream"
crate-type = ["cdylib", "lib"]

[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
