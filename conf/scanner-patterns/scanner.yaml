# Automated Security Scanner / Reconnaissance Tool Detection
patterns:
  - threat_type: "scanner"
    rule_name: "scanner_tool"
    pattern: "(?i)(sqlmap|nikto|nessus|openvas|masscan|nmap|dirbuster|gobuster|wfuzz|hydra)"
    score: 0.9
